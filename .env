# Environment variables for Docker deployment

# JWT Secret - CHANGE THIS IN PRODUCTION!
# Generate with: openssl rand -base64 32
JWT_SECRET=btp3Xx8PjkIup8adl3zx76DVrq6/QFqCLHjZXEaaq/w=

# Optional: Set to 'production' for production deployments
NODE_ENV=production

# Backend port (usually don't need to change)
PORT=3001

# Frontend API URL (only needed if backend is on different domain)
REACT_APP_API_URL=http://localhost:3001
